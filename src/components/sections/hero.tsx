"use client";

import clsx from "clsx";
import styles from "./hero.module.scss";

const Hero = () => {
  return <section className={clsx("section", styles.section)}>Hero</section>;
};

export default Hero;
